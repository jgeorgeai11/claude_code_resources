---
name: "cr_{yyyymmddv##}_{filename}"
goal: Address code quality issues identified in {filepath} to align with {relevant-skills}.
created: {timestamp}
updated: {timestamp}
---

## Implementation Plan

1. [pending] {Category — carries at least one critical/major/minor finding} - `{filepath}`
   - 1.1. [critical|major|minor] Line {N}: {Description}
        - Current: `{current code}`
        - Expected: `{expected code}`
        - Resolution: _pending_
   - 1.2. [suggestion] Line {N}: {Description}
        - Current: `{current code}`
        - Expected: `{expected code}`
        - Resolution: Deferred — {why this is optional / why the current form is acceptable}

2. [pending] {Category — findings are all suggestions; still [pending], since only implementation marks a task completed} - `{filepath}`
   - 2.1. [suggestion] Line {N}: {Description}
        - Current: `{current code}`
        - Expected: `{expected code}`
        - Resolution: Deferred — {why this is optional / why the current form is acceptable}

## Skills with No Issues

1. {Skill name}: No issues found
2. {Skill name}: N/A - {reason}
